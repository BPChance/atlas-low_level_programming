#include "main.h"
/**
 *
 *
 */
void jack_bauer(void)
{
	char hour,minute;
	    for (hour = '0'; hour <= '9'; hour++)
	    {
		            for (minute = '0'; minute <= '9'; minute++)
			    {
				                putchar(hour);
						            putchar(hour);
							                putchar(':');
									            putchar(minute);
										                putchar(minute);
												            putchar('\n');													            }			        }
}
